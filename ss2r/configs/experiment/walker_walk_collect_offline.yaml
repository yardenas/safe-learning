# @package _global_
defaults:
  - walker_walk_simple
  - override /environment: walker

training:
  num_timesteps: 2000000
  # Use high safety budget so that the policy is unsafe
  safety_budget: 12.5

agent:
  store_buffer: true