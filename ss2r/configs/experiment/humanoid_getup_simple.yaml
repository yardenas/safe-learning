# @package _global_
defaults:
  - mujoco_playground_dm_control
  - override /environment: humanoid_getup
  - _self_


training:
  num_timesteps: 5000000
  train_domain_randomization: false
  eval_domain_randomization: false
  episode_length: 300
  safe: false

environment:
  task_params:
    ground_start_probability: 0.9

agent:
  activation: swish