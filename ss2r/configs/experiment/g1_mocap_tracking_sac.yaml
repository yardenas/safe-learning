# @package _global_
defaults:
  - g1_joystick
  - override /environment: g1_mocap_tracking
  - override /agent/penalizer: lagrangian
  - _self_

training:
  train_domain_randomization: true
  eval_domain_randomization: true
  safe: false
